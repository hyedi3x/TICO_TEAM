import createBlockImage from '../imgs/temporaryImgs/작품만들기.png';

const CreateBlock = () => {
    return(
        <div style={{display: 'flex', justifyContent:'center'}}>
            <img src={createBlockImage}/>
        </div>
    )
    
};
export default CreateBlock;